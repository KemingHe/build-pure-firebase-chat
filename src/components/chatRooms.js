// src/components/chatRooms.js
//
// The ChatRooms component.
import React, { useRef, useState } from "react";
import { ref, set } from "firebase/database";
import {
  fireApp,
  fireAuth,
  fireDB,
  fireAnalytics
} from "../fireInit";

export default function chatRooms () {
  
}